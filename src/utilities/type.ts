export type PlanTypes = {
    title: string;
    price: string;
    img: string;
    duration: string;
    discount?: string;
  };
  export type AddOnsTypes = {
    title: string;
    description: string;
    price: string;
    controlName: string;
  }